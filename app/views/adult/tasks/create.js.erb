// const refreshForm = (innerHTML) => {
//   const newReviewForm = document.getElementById('new_review');
//   newReviewForm.innerHTML = innerHTML;
// }

// const addReview = (reviewHTML) => {
//   const reviews = document.getElementById('reviews');
//   reviews.insertAdjacentHTML('beforeend', reviewHTML);
// }

// <% if @task.errors.any? %>
//   refreshForm('<%= j render "tasks/form", restaurant: @restaurant, task: @task %>');
// <% else %>
//   addTask('<%= j render "tasks/show", task: @task %>');
//   refreshForm('<%= j render "tasks/form", restaurant: @restaurant, task: Review.new %>');
// <% end %>

const addTask = () => {
  const newTaskForm = document.getElementById('achievement_task_id');
  newTaskForm.value =
}
